coverage:
  status:
    project:
      default:
        target: auto
        threshold: 5%
    patch:
      default:
        target: 50%

ignore:
  - cmd/**
  - internal/version/**
  - internal/testutil/**
